extends Objective
class_name CollectionObjective

@export var collection_target: PackedScene
@export var max_collection: int

func get_status():
	#check the amount of the item in the inventory
	#find the object in the inventory, check the amount if it exceeds the max
	pass
